*——————— المحاضرة الثانية ———————*

• *المتغير Variable :* موقع في الذاكرة يمكنك من خلاله تخزين قيمة، واستردادها لاحقا عبر اسم المتغير .

• *انواع المتغيرات في Java :*
1) *المتغير المحلي Local :* يُعرف داخل الداله{ } ولايمكن الوصول له من خارجها .
void Omer{ int number; }

2) *المتغير المثيل instance :* يُعرف داخل الكلاس وخارج الدوال{}، اذا كان رقم فأن قيمته الافتراضيه 0، واذا كان boolean فأن قيمته الافتراضيه false .
class Students {
   int Omer ; // متغير مثيل
   void print{ … } };

3) *المتغير الثابت Static :* يُعرف داخل الكلاس وخارج الدوال، قيمته تكون مشتركة لكل الكائنات، ويمكن تغيير قيمته لاحقا (لايمكن ان يكون Local) .
class Students {
  static int id = 5; //المتغير الثابت
  void print() {…} }

4) *المتغير النهائي final :* يُعرف في أي مكان من البرنامج، قيمته ثابته لايمكن تغييرها مره اخرى
final int x = 5 ; // متغير نهائي 

- *تستخدم الكلمة final مع :*
1) المتغيرات : لايمكن تغيير قيمتها .

2) الدوال : عند تعريفها به لايمكن عمل Overriding للداله .
final void Print() {…}

3) الكلاسات : عند تعريفها به لايمكن وراثة الكلاس .
final class Students {…}
——————————————————————
• *الـ String :* هي سلسلة من الاحرف يتم جمعها معا، يتم كتابتها بين علامتي تنصين "O"، وطرقها :
1) باستخدام السلسله النصية[ ]char
2) باستخدام الكلمة String

String text = "Hello, World";
———————————
• *الداله ()length :* للحصول على عدد الاحرف في String .
System.out.print(text.length());
// الناتج: 12
———————————
• *الداله (index)substring :* يطبع كل حروف String بدءا من index حتى نهاية السلسله .
System.out.println(text.substring(7));
// الناتج: World
———————————
• *الداله (index)charAt :* تطبع حرف واحد فقط وهو الموجود في الـindex .
System.out.print(text.charAt(7));
// الناتج: W
———————————
• *الداله ()trim :* تزيل كل المسافات الفارغه في بداية ونهاية الـ String .
String text = "   Hello, World  ";
System.out.print(text.trim());
// الناتج: Hello, World
———————————
• *الداله (oldX,newX)replace :*
- تقوم بتبديل حرف من String بحرف جديد عن طريق الـ index .
System.out.print(text.replace(1, "O"));
// الناتج: HOllo, World

- تقوم بتبديل نص معين من String بنص جديد عن طريق الـنص القديم .
System.out.print(text.replace("Hello", "Hi"));
// الناتج: Hi, World
——————————————————————
• يعتبر من اهم جوانب OOP هو التغليف Encapsulation .

• *محددات الوصول ACCESS MODIFIERS :*
1) الـ public : يمكن الوصول لها من اي مكان .

2) الـ protected : يمكن الوصول لها من نفس الكلاس ومن الكلاسات الاخرى التي لها نفس الـ package عند وراثتها فقط .

3) الـ default : يمكن الوصول لها من نفس الكلاس ومن الكلاسات الاخرى التي لها نفس الـ package فقط .

4) الـ private : يمكن الوصول لها من داخل نفس الكلاس فقط .

🖊️ *———— عــمــر خــالــد بــاوزيــر ————*