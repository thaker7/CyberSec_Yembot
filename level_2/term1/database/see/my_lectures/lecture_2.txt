*——————— المحاضرة الثانية ———————*

• *الـ SQL :* هي لغة تشغيل قواعد البيانات (انشاء وحذف قواعد البيانات وو) .

• الـ SQL تمثل في لغة الكمبيوتر في تخزين البيانات ومعالجتها وتعديلها من قواعد البيانات العلائقيه .

• طورت عام 1970 بواسطة علماء IBM، واصبحت معيار للمعهد الوطني الامريكي (ANSI) عام 1986، والمنظمة الدوليه(ISO) عام 1987 .

• توجد العديد من اللهجات المختلفه للغة SQL مثل (MS SQL Server تستخدم T-SQL) و (Oracle تستخدم PL/SQL) .

• انظمة RDBMS تستخدم لغة SQL كلغة قاعدة البيانات القياسية الخاصة بهم .
——————————————————————
• البيانات في RDBMS المخزنه في كائنات قاعدة البيانات تعرف بـالجداول tables .

• *الـ table :* هو مجموعه من البيانات المدخله المترابطة ويتكون من العديد من الاعمده والصفوف .
• الجدول هو الاكثر شيوعا والابسط لتخزين قواعد البيانات العلائقيه .
table = entity 
——————————————————————
• *انواع اوامر الـ SQL والفرق بينها :*
1) Data Definition Language (DDL) لغة تعريف البيانات
- لتعديل هيكل قاعدة البيانات مثل انشاء/حذف جدول
CREATE TABLE
DROP TABLE

2) Data Manipulation Language (DML) لغة معالجة البيانات
- لادراج وتحديث وحذف البيانات من جداول قاعدة البيانات .
INSERT INTO
UPDATE
DELETE FROM

3) Data Control Language (DCL) لغة التحكم في البيانات
- لادارة حقوق الوصول والتحكم في الاذونات في قاعدة البيانات مثل (GRANT/REVOKE) .

4) Transaction Control Language (TCL) لغة التحكم في المعاملات
- لادارة والتحكم في عمليات المعاملات في قاعدة البيانات مثل (COMMIT/ROLLBACK) .
——————————————————————
• *الـ SQL Syntax :*
- تبدأ العباره بكلمة رئيسيه مثل CREATE - تنتهي جميع العبارات بفاصلة منقوطة - غير حساسة لحالة الاحرف .
- الـ MySQL حساسة لحالة الاحرف في اسماء الجداول .
——————————————————————
• *نموذج BNF لكتابة اوامر SQL :*
- الحروف الكبيرة تمثل الكلمات المحجوزة .
- الحروف الصغيرة تمثل الكلمات المعرفة من المستخدم .
- الـ | : يشير الى اختيار بين البدائل
- الـ ( ) : يشير الى تكرار اختياري 0 أو أكثر .
- الـ { } : تشير ان العنصر مطلوب .
- الـ [ ] : تشير  ان العنصر اختياري .
- القيم الحرفية يجب ان تكون بين علامتي تنصيص مفردة ('عمر')
- القيم الرقيمة يجب الا تكون بين علامتي تنصيص (202250010)
——————————————————————
• *الاجراءات المرجعيه بين الجداول المترابطه :*
1) الـ Cascade : اي تعديل للجدول الاصل يقابله نفس التعديل فالجدول الفرعي .

2) الـ Set Null : اي تعديل للجدول الاصل يقابله قيمه NULL فالجدول الفرعي

3) الـ Set Default : اي تعديل للجدول الاصل يقابله قيمه افتراضيه محدده فالجدول الفرعي .

4) الـ No Atcion : التعديل فقط فالجدول الاصل .
——————————————————————
• انشاء قاعدة بيانات جديدة
CREATE DATABASE nameDB;

• حذف قاعدة بيانات
DROP DATABASE nameDB;

• انشاء جدول وتحديد اعمده له 
CREATE TABLE nametb
(namecol dt, namecol dt);

• استعلام عن كل البيانات الموجودة في الجدول
SELECT * FROM nametb;

• استعلام عن بيانات الاعمده المحدده فقط
SELECT nmcol, nmcol2
FROM nametb;

• استعلام عن كل بيانات العمود المحدد، دون تكرار قيمة الصفوف
SELECT DISTINCT nmcol
FROM nametb;

• ضرب كل صفوف العمود المحدد ×3
SELECT  nmcol×3
FROM nametb;

• عرض اسم العمود المحدد باسم مختلف .
SELECT nmcol AS newnm
FROM nametb;

• ادراج صفوف في جدول على صف صف
INSERT INTO NmTb
VALUES(12, 'OMER');
- شروطها: ان تكون عدد العناصر المدرجه ونوع بياناتها يطابق عدد العناصر الموجوده في الجدول ونوع بياناته .

• تعديل/تحديث بيانات عمود في جدول (يجب ان تكون نوع البيانات المعدله مطابقة للقديمه)
UPDATE NameTable
SET nmcol = nmcol×2;

• حذف جميع الصفوف من جدول
DELETE FROM NameTable;

• حذف صفوف من جدول بناء على شرط معين
DELETE FROM NameTable
WHERE nmcol = 'ali';

• تستخدم View مثل فنكشن لعدم تكرار الاوامر وتستدعى باسمها
CREATE VIEW vwname as
SELECT nmcol, nmcol
FROM nametb;

• لتحديد الجدول الذي سيتم استخدامه FROM .
• تستخدم لوضع شروط معينه لفلترة الصفوف WHERE .
• تشكيل مجموعات من الصفوف بنفس قيمة العمود GROUP BY .
• مجموعات تخضع لبعض الشروط HAVING .
• تحديد الاعمدة التي ستظهر في الاخراج SELECT .
• تحديد ترتيب الاخراج ORDER BY
- لايمكن تغيير ترتيب الجمل

🖊️ *———— عــمــر خــالــد بــاوزيــر ————*